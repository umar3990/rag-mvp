# Copy this file to .env and fill in real values. .env is gitignored —
# never commit real keys.

# Postgres connection (matches docker-compose.yml)
DATABASE_URL=postgres://rag_mvp:rag_mvp@localhost:5433/rag_mvp_development

# Used for embeddings + chat completion (Day 5+). Get one from
# console.anthropic.com or platform.openai.com depending which we pick.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
